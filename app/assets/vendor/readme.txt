Save vendor libs (eg. jquery.min.js) here. All *.js files will
be picked up automatically.

They'll be aggregated into bundle.js, and automatically included
toward the end of <body>, so that they load after the DOM but
before the contents of app/assets/scripts/*.coffee files.
